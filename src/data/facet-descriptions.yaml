# Descriptions shown under the "Required" / "Best practice" filter facets
# in the metadata dictionary UI. Not sourced from the API; curated by hand.
metadataBlock: |
  A metadata block groups the relevant metadata fields
required: System required fields to publish a dataset.
bestPractice: |
  Fields Dataverse doesn't require, but the Dataverse North Metadata Best Practices Guide recommends or notes as optional
